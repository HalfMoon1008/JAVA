package algorithm;
import java.util.*;
import java.io.*;



public class day2_11656 {

	public static void main(String[] args)/*throws IOException*/ {

		/*
		Scanner sc = new Scanner(System.in);
		System.out.println("문장을 입력해주세요 : ");
		String s = sc.nextLine();      //입력된 글자수를 N이라고 함
		int n = split(s);
		String[] a = new String[n];    //N개 만큼 배열을 만듬
		
		for(int i=0; i<n; i++) {
			a[i] = s.substring(i);     //배열 a의 인덱스 i에 i이후의 문장이 오게함
		}
		Arrays.sort(a);				   //배열 a를 정렬함
		System.out.print("결과값 : ");
		for (int i=0; i<n; i++) {	 
			System.out.print(a[i]+'|');   //하나씩 프린트
		*/
		
		/*
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));		//입력
	    BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));		//출력
	    String[] s = br.readLine().split(""); //배열 s에 입력값을 쪼개서 넣음
	    String[] answer = new String[s.length]; //배열 answer에 s의 길이만큼 배열을 만듬

	    for(int i = 0; i < s.length; i++) {
	      StringBuilder sb = new StringBuilder();
	      for(int j = i; j < s.length; j++) {
	        sb.append(s[i]);				//쪼개진 글자를 다시 묶음
	      }
	      answer[i] = sb.toString();
	    } 
	    Arrays.sort(answer);

	    for(String str : answer) {
	      bw.write(str + "\n");
	    }
	    bw.flush();
	    bw.close();*/
	    
		
	}

}
